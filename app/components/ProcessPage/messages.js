/*
 * ProcessPage Messages
 *
 * This contains all the text for the ProcessPage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ProcessPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProcessPage component!',
  },
});
