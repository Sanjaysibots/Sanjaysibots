/*
 * WorkPage Messages
 *
 * This contains all the text for the WorkPage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WorkPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WorkPage component!',
  },
});
