/*
 * ExpertisePage Messages
 *
 * This contains all the text for the ExpertisePage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ExpertisePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExpertisePage component!',
  },
});
